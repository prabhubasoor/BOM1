# Australian Government Bureau of Meteorology web system test using Playwright

## Prerequisites to running tests
# Install NPM packages and Node.js
`npm install Node.js`

# Install playwright and dependencies 
`npm install`

## Run tests
To run all test:
`npm run test`